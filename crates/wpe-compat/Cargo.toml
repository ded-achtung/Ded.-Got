[package]
name = "wpe-compat"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "SupportMatrix, LoadReport, CompatWarning — shared types for wpe-rs"
